# This file contains the environment variables that the backend needs to run.
# You need to create a .env file in the same directory and copy the contents of this file into it.
# Replace the values with your own.

DB_HOST=localhost
DB_USER=your_username
DB_PASSWORD=your_password
DB_NAME=your_database

MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=your_mongodb_name
